# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
.hypothesis/

# Environments
.venv/
venv/

# uv (the lockfile IS committed; the cache is not)
.uv/

# Generated artifacts
sbom.cdx.json
*.sbom.json
audit-report.md
requirements-audit.txt
STATUS.md
STATUS.json
dashboard/site/index.html
dist/
build/
# coverage data (the lib lane runs pytest --cov; report-only, never committed)
.coverage
.coverage.*
htmlcov/

# Agent runtime logs (PostToolUse log.sh)
.claude/logs/

# Drift-audit per-run history line (uploaded as a CI artifact, never committed)
audit-history.ndjson

# OS / editor
.DS_Store
*.swp
